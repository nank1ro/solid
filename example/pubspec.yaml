name: example
description: Smoke-test app for the Solid v2 generator.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.9.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_solidart: ^2.7.3
  solid_annotations:
    path: ../packages/solid_annotations

dev_dependencies:
  build_runner: ^2.14.0
  flutter_test:
    sdk: flutter
  solid_generator:
    path: ../packages/solid_generator
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
