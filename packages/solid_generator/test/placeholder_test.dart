// Placeholder so `dart test` exits 0 instead of 79 (no tests ran).
// Replaced by the real integration suite in TODO M0-06.
import 'package:test/test.dart';

void main() {
  test('placeholder', () {});
}
