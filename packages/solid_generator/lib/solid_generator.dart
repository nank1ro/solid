/// solid_generator — source-to-lib code generator for Flutter reactive state.
///
/// This package ships as a `build_runner` builder. End users do not import
/// from this library directly; the builder is invoked automatically via
/// `dart run build_runner build` (or `dart run build_runner watch`).
///
/// The build_runner entry point that actually wires the builder is
/// `package:solid_generator/builder.dart` (referenced from `build.yaml`).
///
/// See the package README for installation and usage:
/// https://pub.dev/packages/solid_generator
library;
